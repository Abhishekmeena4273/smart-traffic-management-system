services:
  - type: web
    name: smart-traffic-management
    env: java
    rootDir: .
    plan: free
    buildCommand: mvn clean package -DskipTests
    startCommand: java -Xmx512m -XX:+UseG1GC -jar target/smart-traffic-management-1.0.0.jar
    healthCheckPath: /api/health
